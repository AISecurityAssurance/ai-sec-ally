rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user (including your service account)
    // to read and write to the database.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
